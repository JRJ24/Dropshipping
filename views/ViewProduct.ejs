<%- include('layouts/header') %>
<div class="container">
        <div class="row mb-4">
            <div class="col-lg-12">
                <div class="table-responsive">
                    <table class="table table-striped text-center">
                        <thead class="table-dark">
                            <tr>
                                <th scope="col">ID</th>
                                <th scope="col">Nombre</th>
                                <th scope="col">Descripción</th>
                                <th scope="col">Categoría</th>
                                <th scope="col">Stock</th>
                                <th scope="col">Precio</th>
                                <th scope="col">Foto</th>
                                <th scope="col">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% products.forEach((products, ViewProduct) => { %>
                                <tr class="align-middle">
                                    <td><%= ++ViewProduct %></td>
                                    <td><%= products.name %></td>
                                    <td><%= products.description %></td>
                                    <td><%= products.category %></td>
                                    <td><%= products.stock %></td>
                                    <td><%= products.price %></td>
                                    <td><img src="/upload/<%= products.image %>" width="50" class="img-thumbnail"></td>
                                    <td>
                                        <a href="/products/edit/<%= products._id%>" class="fa fa-edit fa-lg"></a>
                                        <a href="/products/delete/<%= products._id%>" class="fa fa-trash fa-lg"></a>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

<%- include('layouts/footer') %>
