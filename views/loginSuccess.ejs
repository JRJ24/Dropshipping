<%- include('layouts/header') %>
<div class="container py-5">
  <h1>Bienvenido, <%= user.name %> 👋</h1>
  <p>Guardando tu sesión en este navegador…</p>
</div>

<script>
  // Insertamos un literal JSON seguro. Evitamos cerrar etiquetas <script> con este reemplazo.
  const userToken =  JSON.stringify(user).replace(/</g, '\\u003c') ;
  localStorage.setItem('user', JSON.stringify(userToken));
  // Redirige donde prefieras:
  window.location.href = '/ViewProduct';
</script>
<%- include('layouts/footer') %>
